import { Fragment } from "react";
import Movies from "../components/Movies";

const Avoir = () => {



    return(
        <Fragment>
            <h1>Liste de vos films à voir</h1>
            {/* {
                Avoir.map( item => <Movies key={item.id} datas={item} />)
            } */}
        </Fragment>
    )
}

export default Avoir;